import React from "react";

const Pagination = ({ page, isNext,containerClasses }: { page: string; isNext: boolean, containerClasses:string }) => {
  return <div>{page}</div>;
};

export default Pagination;
